Help me add a new project to the portfolio:

1. Ask for project details:
   - Project title
   - Short description (one line)
   - Long description (for modal, optional)
   - Technologies/tags used
   - Demo URL (optional)
   - Repository URL (optional)
   - Image filename (will be placed in /assets/)

2. Generate properly formatted JSON entry

3. Add to `mydata.json` projects array in the correct position

4. Remind me to:
   - Add the project image to `/assets/` directory
   - Image should be 800x600px recommended
   - Supported formats: JPG, PNG, SVG, WebP

5. Show preview of the new project entry

6. Validate the entire JSON file after adding
